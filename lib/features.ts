/**
 * Feature flags for frontend surfaces.
 * Keep in sync with leadhub-backend APOLLO_LEAD_SEARCH_ENABLED.
 * Set APOLLO_UI_ENABLED to true to restore Apollo search/sync/key UI.
 */
export const APOLLO_UI_ENABLED = false;
