// ThemeToggle is no longer needed - single light theme
// Keeping this file for backwards compatibility if imported elsewhere

"use client";

import React from "react";

// This component is deprecated - we only use light theme now
const ThemeToggle: React.FC = () => {
  // Return null - no theme toggle needed
  return null;
};

export default ThemeToggle;
